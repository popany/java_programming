server:
  port: 50000

# DataSource Config
spring:
  datasource:
    driver-class-name: org.h2.Driver
    schema: classpath:../test-classes/db/schema.sql
    data: classpath:../test-classes/db/data.sql
    url: jdbc:h2:mem:test
    username: root
    password: test